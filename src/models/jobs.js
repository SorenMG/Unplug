import moment from 'moment';
import Api from '../lib/api';
import initialState from '../store/jobs';

export default {
  namespace: 'jobs',

  /**
   *  Initial state
   */
  state: initialState,

  /**
   * Effects/Actions
   */

  /**
   * Reducers
   */
};
